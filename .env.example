# Для живого AI-разбора нужен серверный ключ. Без него работает объяснение по правилам.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Необязательный путь к базе результатов команд (по умолчанию backend/data/scenarios.sqlite3).
# SIMULATOR_DB_PATH=backend/data/scenarios.sqlite3
